# 021 · 代码结构

> **状态**：✅ —— HTTP 从 `main.py` 按领域拆到 `app/routers/`，人名标签从 `store.py` 拆到 `people.py`。行为不变，`uv run pytest` 310 passed。
> **范围**：只搬家，不改口径、不改接口、不改前端、不上 TypeScript。
> **前置**：复评里结构分从 6.0 掉到 5.8，是因为 `main.py` 1200→1879、`store.py` 900→1546。010 把「main.py 过大」划过后排；本文件把它做完。

# ✅ 已完成：HTTP 按领域拆路由

`app/main.py` 只留启动、异常处理和静态文件（约 90 行）。连接和当前账号在 `app/deps.py`。路由按原来的注释边界挂在 `app/routers/`，注册顺序不变（先 `kingdom/gear` 再 `kingdom/{id}`）。

入口仍是 `uvicorn app.main:app`。

# ✅ 已完成：人名标签离开 store

`app/people.py` 管谁喝的。`Conflict` 放到 `app/errors.py`。`store.py` 仍再导出 `list_people` / `ensure_person` 等，旧 `import` 不用改。`store.py` 回到约 1430 行，只剩豆子、袋子、冲煮事件。

# ⌛️ 未完成：前端大页和 TypeScript

`BeanCard.jsx` 仍约 1280 行，全站仍是 JSX。010 后排那句「前端无 TypeScript」还在，本轮不动。

**验收**：`uv run pytest` 310 passed。手测见 [006](006-🚧-手工验收.md)，机器已钉、不必测。
