# 001 · 仓库协作框架初始化

> **状态**：✅ 框架已关单（2026-09-04）。产品定位与架构已转到 [docs/002-🚧](002-🚧-豆子档案与小主机架构.md)，不阻塞本单。

本仓库从空 README 起步，对齐既有协作习惯：工作牌、AGENTS、CLAUDE、todo、编号任务看板。

---

# ✅ 已完成：克隆远程并落本地目录

- **范围**：把 [https://github.com/dpmoehre/coffeebar.git](https://github.com/dpmoehre/coffeebar.git) 初始化到本机文件树，不混入其他工程仓库。
- **实现**：克隆至 `/Users/dp/Documents/001_gitea/dpmoehre/coffeebar`（`001_gitea/<org>/<repo>` 惯例）。远程已有 `Initial commit`（仅 `# coffeebar`）。
- **验收**：目录含 `.git`，`origin` 指向上述 GitHub 地址，当前分支 `main`。

# ✅ 已完成：工作牌 / Agent / Claude / todo 框架

- **工作牌**：`.cursor/rules/project-framework.mdc`（alwaysApply 身份与流程）+ `.cursor/rules/queued-task-then-push.mdc`（队列任务完成即提交推送）。
- **Claude**：根目录 `CLAUDE.md` 为规则唯一正文（项目定位、Git、任务看板、命名、摘要）。
- **Agent**：根目录 `AGENTS.md` 只引用 CLAUDE.md，不复制正文。
- **todo**：根目录 `todo.md` 按日期倒序索引；本轮看板为本文件。
- **配套**：`README.md` 改为功能/使用说明；`_Doc/README.md` 说明人工文档目录；`.gitignore` 排除本机垃圾与密钥。
- **验收**：上述路径均在仓库根可见；规则交叉引用可点开；无产品代码、无密钥。

# ✅ 已完成：产品定位移交 002（不在本单实现）

- 产品定位、小主机环境与技术栈已记在 [docs/002-🚧-豆子档案与小主机架构.md](002-🚧-豆子档案与小主机架构.md)。
- 应用脚手架与豆卡功能仍属 002 未完成项，不回溯本单。
