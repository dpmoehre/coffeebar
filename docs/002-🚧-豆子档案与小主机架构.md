# 002 · 豆子档案与小主机架构

> **状态**：🚧 架构已落档（2026-09-04），应用脚手架与业务代码尚未开工。
> **范围**：公司咖啡吧自用工具的产品定位、运行环境、软件架构。不是点单点菜系统。
> **记录依据**：本轮对话对齐；用户要求正文直接写在 `docs/`，不另开 `_Doc/` 副本。

---

# ✅ 已完成：产品定位与运行环境

## 这是什么

公司里的个人手冲 / 调酒吧台。用途是**自己做手冲咖啡、偶尔调酒时，把豆子档案管清楚**。

第一期只做豆子：

- 豆卡：产地、风味、当前克重
- 每次消耗记流水（冲一次扣多少克）
- 冲煮指导（怎么冲）
- 拍照上传豆子图片（主要用手机）

酒水与调酒配方留到第二期，表结构预留品类扩展，第一期不做界面。

## 明确不是

- 不是给同事点单、排队、支付的系统
- 不是后厨 KDS
- 不是多门店 / 公司 SSO

## 谁在哪用

| 场景 | 设备 | 怎么用 |
| ---- | ---- | ---- |
| 日常查豆、改豆卡、记消耗、看冲煮指导 | 小主机自带屏幕 + 鼠标键盘 | 本机浏览器打开网页，桌面布局为主 |
| 拍豆卡照片并上传 | iPhone（必要时 iPad） | 同一套网页的拍照/选相册页 |
| 偶尔换一台电脑看 | 公司内网里的 Mac / Windows / iPad | 浏览器访问小主机内网地址 |

手机不是主操作台，只在「需要照片」时上场。

## 小主机（唯一服务器）

| 项 | 值 |
| -- | -- |
| 系统 | Windows 11 |
| CPU | Ryzen 7 PRO 7850H + Radeon Graphics |
| 内存 | 16 GB |
| 交互 | 自带屏幕，平时本机键鼠操作 |
| 网络 | 公司内网为主；必要时用 **cpolar** 做临时外网代理 |
| 角色 | 既是服务器，也是日常使用的工作台 |

这台机器算力充裕，架构按**单机单体**做即可，不必上集群、不必上独立对象存储。16 GB 同时跑 Win11 桌面 + 本应用足够；**默认不用 Docker Desktop**（少占内存、少一层 WSL2），本机用 `uv` / `npm` 跑进程，开机自启。

---

# ✅ 已完成：软件架构方案

## 形态

一套响应式网页，一台小主机跑完。不写四套原生 App。

```
[日常] 小主机屏幕上的 Edge/Chrome  ──localhost──┐
[拍照] iPhone / iPad 浏览器          ──内网 IP──┤
[偶尔] 其他 Mac / Windows            ──内网 IP──┼── Caddy :80 ── FastAPI ── SQLite
[必要] 外出临时访问                  ──cpolar──┘                 └── data/photos/
```

小主机上只保留三件事：

1. **反向代理**：Caddy（本机 80，转给应用端口）
2. **应用进程**：FastAPI 提供 API，并托管前端构建产物（或 Vite 开发时拆开）
3. **数据**：SQLite 单文件 + 本地图片目录

不引入微服务、消息队列、云 OSS、独立用户中心。

## 技术栈（已选定）

与仓库约定一致：Python 用 **uv**，前端用 **npm**，密钥与运行数据不入库。

- **前端**：Vite + React。**桌面优先**（对齐小主机屏幕 + 键鼠）；拍照上传页单独按手机宽度做好。
- **后端**：FastAPI（`uv`）。豆卡 CRUD、图片上传与转码、库存流水、冲煮指导。
- **数据库**：SQLite（例如小主机上的 `data/coffeebar.db`）。备份 = 拷文件。
- **图片**：`data/photos/`，不进 Git。iPhone 常见 HEIC，服务端转 JPEG/WebP 并生成缩略图。
- **入口**：内网 `http://小主机名或局域网IP`；本机也可 `http://localhost`。
- **进程**：Windows 开机启动（任务计划或 NSSM 一类服务），不要依赖每次手动开终端。
- **口令**：环境变量注入的简单口令，防止同事误改库存。cpolar 打开时必须有口令，且 token / 口令不入库。

备选（未采用）：Next.js 全栈。功能等价，但和「Python 用 uv」分叉，除非后续明确改口。

## 第一期信息结构

```
Bean 1──* BeanPhoto
Bean 1──* InventoryEvent    # 入库 / 消耗 / 校正，单位克
Bean 1──* BrewGuide         # 可多套器具或多套手法
```

豆卡字段（第一期）：

- 名称、产地、处理法、烘焙度、烘焙日期、风味描述
- 当前克重（由流水汇总，允许记一笔「校正」）
- 照片（至少一张主图）
- 冲煮指导：器具、粉量、水量、研磨、水温、步骤文本

页面保持少：

1. **豆库**（桌面主页）：图、名字、剩余克重、风味标签
2. **豆卡**：产地 / 风味 / 库存 / 照片 / 冲煮指导 / 消耗记录
3. **拍照补图**（手机）：相机或相册上传
4. **冲一次**：填本次克重，写一条消耗流水

「每次消耗量」做成流水，不只改一个数字：能看当前剩多少，也能回看哪天用了哪只豆、用了多少克。

## 网络

- **默认**：只走公司内网。小主机与手机连同一局域网即可上传照片。
- **cpolar**：仅在需要外网打开时临时开隧道，指向本机 Caddy/应用端口。不把 cpolar 写成常驻架构，不把隧道地址或 token 写进仓库。
- 开隧道期间视为半公网：口令必开；能关则关。日后若改固定域名再谈正式 HTTPS，不阻塞第一期。

## 数据落点与备份

- 代码在本仓库；**运行数据只在小主机 `data/`**，`.gitignore` 排除。
- 备份：定期拷贝 `data/coffeebar.db` + `data/photos/` 到另一块盘或个人网盘。
- 照片不走云 OSS，避免公司环境绑外网账号。

## 第一期不做

- 点单、排队、支付、KDS
- 公司 SSO、多角色权限
- 常驻公网、复杂证书体系（cpolar 只作临时通道）
- 酒水库存与调酒配方界面（仅预留 `kind=bean|spirit` 一类扩展位）

## 验收（本轮：架构文档）

- 本文写清定位、硬件、桌面主用 / 手机只拍照、内网 + 可选 cpolar、单体技术栈与数据模型。
- [CLAUDE.md](../CLAUDE.md)「项目定位 / 架构概览 / 环境与工具」与根 [README.md](../README.md) 已回写，不写日期流水账。
- 无业务代码、无密钥、无 `data/` 入库。

---

# ⌛️ 未完成：应用脚手架与第一期功能

不在本轮提交里做。下一张任务单再落地：

- Windows 11 上 `uv` + FastAPI、`npm` + Vite/React、Caddy、开机自启
- 豆库 / 豆卡 / 拍照上传 / 消耗流水 / 冲煮指导
- HEIC 转码与缩略图
- 本机屏幕走桌面布局、手机走拍照页的实机验收
