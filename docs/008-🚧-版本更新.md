# 008 · 版本更新记录

> **状态**：🚧 —— 侧栏「更新」页与白话条目已落地；小主机手测未收。
> **范围**：给吧台的人看「这次改了什么」。不写 git / pytest / 文件名。不替代 `todo.md` 或编号任务文档。

# ✅ 已完成：侧栏「更新」页

侧栏（窄屏顶部图标）增加「更新」。正文来自 [`web/src/changelog.js`](../web/src/changelog.js)，日期倒序，每条一个短标题 + 几句白话。主栏不再锁 `1480px`，铺满侧栏右边；宽屏两条并排。

**约定**：用户能看见的改动，收尾时用一两句人话追加到该文件**最上面**。不要放进任何叫 `data/` 的目录（运行库不入库）。`todo.md` 仍给 Agent 索引，README 仍只写功能说明。

**验收**：`node --test web/src/changelog.test.js` 钉住倒序和字段。手测在 [006](006-🚧-手工验收.md)。

# 🚧 进行中：小主机手测更新页

打开侧栏「更新」，应能看到今天这条（地图能筛层、有了更新记录）以及更早的地图 / 删卡 / 登录等条目。
