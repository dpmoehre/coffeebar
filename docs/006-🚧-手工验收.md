# 006 · 手工验收

> **状态**：🚧 —— 长期账本。每次改完交给用户时在这里追加一轮测试集合，用户回报后改状态。
> **约定**：详见 [CLAUDE.md](../CLAUDE.md)「交给用户时必须给手工测试集合」。`todo.md` 只引用本文件，不复制清单。

标记：`待测` / `已测通过` / `未测` / `失败` / `机器已钉、不必测`。

# 🚧 进行中：2026-09-06 地图缩小钉子不跑偏

对应 [007](007-🚧-豆子地图.md)。机器：缩小不再把分得开的钉拉成一圈，已钉、不必测。

- **这台 Mac 开发机**：打开 [http://localhost:8000](http://localhost:8000)，前端已重编 `web/dist`。走 cpolar 的请硬刷新。
- **只看、可定点。【测试】卡才能手点。不要改真豆价钱。**

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 打开地图，滚轮缩小到能看见整块非洲 | 埃塞 / 卢旺达那些钉子还在对应国土上，不要漂到海里 | 待测 |
| 2 | 再放大回耶加一带 | 挤在一起的钉能分开点；钉本身不要跟着放大成饼 | 待测 |

# 🚧 进行中：2026-09-06 拍豆卡 MCP 建档

对应 [015](015-✅-拍豆卡MCP建档.md)。机器：带小 JPEG 建卡会挂 `card` 图、路径无效卡仍在、查重不跨账号、同名不拦、只给浅烘不会扫到全部浅烘，已钉、不必测。

- **这台 Mac 开发机**：服务要开着（[http://localhost:8000](http://localhost:8000)）。改完 MCP 工具后**完全退出 Cursor 再开**，才能看见 `find_similar_beans` 和带图的 `create_bean`。本机 `data/` 和小主机不是同一份库。
- **只用虚构豆卡图或【测试】豆练手。不要对瑰夏村 / 南米纳斯 / MATYAZO / 晨曦焦糖重复建档，也不要动真库存价钱。**

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 在 Cursor 丢一张虚构豆卡图，说要建档 | Agent 先列出读到的字段和是否撞车；没让写就不要出现新卡 | 待测 |
| 2 | 看完字段后说「写入」 | 豆库多一张卡，详情里有那张图（豆卡纸 `card`） | 待测 |
| 3 | 再丢同一张图说要建档 | Agent 先问是再入一袋还是仍要新建，不要直接再建一张 | 待测 |
| 4 | 克重或价钱在图上读不清时 | 标待入袋 / 空着，不要编数字 | 待测 |

# ✅ 已完成：2026-09-06 代码结构拆分

对应 [021](021-✅-代码结构.md)。机器：路由搬家后 `uv run pytest` 310 passed，不必测。页面看起来应和拆之前一样；若登录或豆库 404，是服务没重启，不是口径变了。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 本机改完后端后重启，打开豆库 | 和拆之前一样能看见自己的豆 | 机器已钉、不必测 |

# 🚧 进行中：2026-09-06 王国器具

对应 [020](020-🚧-咖啡王国.md)。机器：收进目录即进王国、一人一件可改可撤、别人账号看不见「（我）」、注销留目录、广场对跳，已钉、不必测。

- **这台 Mac 开发机**：打开 [http://localhost:8000](http://localhost:8000)，改完后端要重启（新表 `kingdom_gear_score`），改完前端已重编 `web/dist`。走 cpolar 的请硬刷新。本机 `data/` 和小主机不是同一份库。
- **只拿【测试】器具练手。不要动轰炸机滤纸和真库存价钱。**

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 打开侧栏「王国」 | 标题下有「豆子 / 器具」芯片 | 待测 |
| 2 | 点「器具」（目录里已有件时） | 能看见已收录的滤杯/壶；点开能评总体分、写一句、收藏 | 待测 |
| 3 | 若有广场公开且已进目录的【测试】器具，点开 | 有「王国里的同一件」，点「去评价」进王国这件 | 待测 |
| 4 | 王国器具页点「领到台面」 | 自己器具页多一件；再点变成「已在台面」 | 待测 |
| 5 | 豆卡王国页仍只有杯测八维 | 器具评价没有干香/酸质那些格子 | 待测 |

# 🚧 进行中：2026-09-06 豆库今天条

对应 [013](013-✅-吧台今天条.md)。机器：凌晨 3 点算前一天、撤回不上芯片、别人账号看不见、整袋补录不算一杯，已钉、不必测。

- **这台 Mac 开发机**：打开 [http://localhost:8000](http://localhost:8000)，改完后端要重启（新接口 `GET /api/today`），改完前端已重编 `web/dist`。走 cpolar 的请硬刷新。本机 `data/` 和小主机不是同一份库。
- **只看、点跳转。不要在这条上记消耗，也不要动真库存价钱。** 没有今天的冲煮时整条不出现，属正常。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 打开豆库（在库） | 标题和「在库 / 历史」之间，有今天的人 / 赏味 / 补货 / 上一杯才出条；四块不必齐 | 待测 |
| 2 | 切到「历史」「全部」，再切回「在库」 | 今天条还在，不跟着列表范围消失 | 待测 |
| 3 | 点一个人名芯片 | 进日历，并滤成这个人 | 待测 |
| 4 | 点赏味或上一杯里的豆 | 打开那张豆卡 | 待测 |
| 5 | 若有补货块，点「N 支见底」或「N 条要补」 | 进补货页 | 待测 |
| 6 | 进豆卡、酒库、统计 | 这些页顶上没有今天条 | 待测 |

# 🚧 进行中：2026-09-06 日期格子日历图标

对应 [008](008-🚧-版本更新.md)。机器：无接口。

- **这台 Mac 开发机**：打开 [http://localhost:8000](http://localhost:8000)，前端已重编 `web/dist`。走 cpolar 的请硬刷新。
- **不改数据。**

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 打开新建豆子或豆卡上的购入日 / 烘焙日格子 | 右边小日历是奶油琥珀色，和黑底分得开；点一下能弹出选日期 | 待测 |

# 🚧 进行中：2026-09-06 列表封面一张张出来

对应 [008](008-🚧-版本更新.md)。机器：列表封面多一档 `s_`、上传会带上，已钉、不必测。

- **这台 Mac 开发机**：打开 [http://localhost:8000](http://localhost:8000)，改完后端要重启，改完前端已重编 `web/dist`。走 cpolar 的请硬刷新。
- **不改数据。**

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 走 cpolar 打开豆库（可硬刷新） | 先看到卡片名字和剩余，封面一张张淡入，不要等全部图好了才出字 | 待测 |
| 2 | 再看器具 / 酒水 | 同样是底纹先在，图随后补上 | 待测 |
| 3 | 点进一张豆卡，再返回豆库 | 卡片和已经看过的封面马上在，不要再从底纹一张张重来 | 待测 |

# 🚧 进行中：2026-09-06 滤纸当耗材

对应 [018](018-🚧-个人器具与冲煮建议.md)。机器：没开包不加纸钱、开包后冲一杯扣一张、撤回加回、多包不自挑、整袋补录不扣、补货 ≤20、广场领回不带张数，已钉、不必测。

- **这台 Mac 开发机**：打开 [http://localhost:8000](http://localhost:8000)，改完后端要重启（新表 `filter_pack`、消耗表新列、器具类型 CHECK），改完前端已重编 `web/dist`。本机 `data/` 和小主机不是同一份库。
- **只拿【测试】滤纸和【测试】豆卡练手。不要给轰炸机那包编造剩余，也不要动真库存价钱和消耗。** 测完删【测试】卡。硬刷新后再看。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 器具页登记一张【测试】滤纸，先别开包。用【测试】豆冲一杯 | 这杯钱只有豆钱；滤纸卡上写「还没开始计张」；补货页没有这张滤纸 | 待测 |
| 2 | 打开这张滤纸，点「开一包」，写 10 张 / 10 元 | 卡上变成还剩 10 张；不要去改轰炸机那张 | 待测 |
| 3 | 再用【测试】豆冲一杯（V60） | 滤纸剩 9 张；这杯钱比刚才多约 1 元；流水写「含滤纸」 | 待测 |
| 4 | 撤回刚才那杯，再恢复 | 撤回后剩 10 张；恢复后又剩 9 张 | 待测 |
| 5 | 轰炸机滤纸（真库存）只看、别开包、别估剩余 | 仍是「还没开始计张」或备注里的枚数，账面张数没有 | 待测 |

# 🚧 进行中：2026-09-06 手机字号

对应 [008](008-🚧-版本更新.md)。机器：根字号和页标题类名已改，无接口。

- **这台 Mac 开发机**：打开 [http://localhost:8000](http://localhost:8000)，前端已重编 `web/dist`。手机走 cpolar 的请硬刷新。
- **不改数据。**

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | iPhone 打开「更新」 | 「更新」和卡片标题比改前小一档，正文不再顶满屏；一屏能多看见几条 | 待测 |
| 2 | 同一部手机再看豆库 / 器具 | 页标题同样小一档，功能按钮都在 | 待测 |
| 3 | 电脑宽屏打开同一页 | 标题和以前差不多大 | 待测 |

# 🚧 进行中：2026-09-06 空库进门耶加雪菲

对应 [005](005-🚧-社区版与多租户.md)。机器：空库送耶加雪菲、接手不送、练习豆不算真库存号、注销不用导出，已钉、不必测。

- **这台 Mac 开发机**：打开 [http://localhost:8000](http://localhost:8000)，改完后端要重启（新列 `bean.seed`）。本机 `data/` 和小主机不是同一份库。
- **用一个没用过的邮箱注册。不要在小主机真库存上试 leave 或注销。**

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 打开注册页，先别点按钮 | 副文案写进门有耶加雪菲，没有「没主人的库存」 | 待测 |
| 2 | 用新邮箱注册进豆库 | 有一张「耶加雪菲」，产地埃塞俄比亚，练习袋 100 g，没有价钱，封面是包装图 | 已测通过 |
| 3 | 这个号侧栏注销，不要先下载备份 | 能直接注销；再登录失败 | 待测 |

# 🚧 进行中：2026-09-06 烘焙日与赏味窗

对应 [016](016-✅-烘焙日与赏味窗口.md)。机器：窗口边界、未来日期拒、杯测快照不回溯、广场只露天数不露哪袋还剩多少，已钉、不必测。

- **这台 Mac 开发机**：打开 [http://localhost:8000](http://localhost:8000)，改完后端要重启（新列），改完前端已重编 `web/dist`。本机 `data/` 和小主机不是同一份库。
- **只拿【测试】豆练手。不要动真库存价钱和消耗。** 测完删或改回私密。硬刷新后再看。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 新建或打开一支【测试】豆，给在喝的那袋填烘焙日（比如十天前），点记下 | 袋子上出现「烘后第 N 天 · 正当时」（或养豆中，看你填的日子和烘焙度） | 待测 |
| 2 | 回豆库，排序改成「新鲜」 | 刚填过烘焙日的在前面；没填的在最后，不会消失 | 待测 |
| 3 | 点芯片「正当时」，再点「没填烘焙日」 | 先只剩正当时的；再点变成只看没填的；「清除筛选」后标签和阶段都松开 | 待测 |
| 4 | 进这张豆卡，杯测区打一杯（带上烘焙日） | 雷达下有「烘后第 N 天 · …」；再把袋子烘焙日改成更早，这行旧分的天数不变 | 待测 |

# 🚧 进行中：2026-09-06 广场领回豆子和器具

对应 [005](005-🚧-社区版与多租户.md) / [018](018-🚧-个人器具与冲煮建议.md)。机器：默认私密不上广场、领回拷图不带袋子、自己的不能领、再领一次还同一张，已钉、不必测。

- **这台 Mac 开发机**：打开 [http://localhost:8000](http://localhost:8000)，改完后端要重启（新列），改完前端已重编 `web/dist`。本机 `data/` 和小主机不是同一份库。
- **只拿【测试】豆卡和【测试】器具练手。不要动真库存价钱、消耗和轰炸机滤纸。**

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 用 A 号把一件【测试】器具改成公开，换 B 号进广场「器具」 | 能看见这件；点开「领到我的台面」后 B 的器具页多一件，图还在 | 待测 |
| 2 | B 再点一次领 | 还是同一件，不会再多一条 | 待测 |
| 3 | A 号把一张【测试】豆卡公开（可带照片），B 点「收到我的豆库」 | B 豆库多一张同名卡，没有袋子、没有还剩多少；原卡还在 A 那里 | 待测 |
| 4 | A 在广场点自己的公开卡/器具上的领 | 提示不用领，不会复制一份给自己 | 待测 |

# 🚧 进行中：2026-09-06 广场克价与排序

对应 [005](005-🚧-社区版与多租户.md)。机器：`offer.per_g`、按克价/袋价/烘焙/产地排，已钉、不必测。

- **这台 Mac 开发机**：打开 [http://localhost:8000](http://localhost:8000)，改完前端已重编 `web/dist`。本机 `data/` 和小主机不是同一份库。
- **不要动真库存的价钱和消耗。** 用已公开的卡或【测试】卡看即可。硬刷新后再看。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 侧栏进「广场」 | 填过价和克重的卡上能看见「200 g · ¥88 · 0.44 元/g」 | 待测 |
| 2 | 搜索框旁边改成「克价低」 | 有价的卡按每克从便宜到贵；没填价的在后面 | 待测 |
| 3 | 改成「烘焙」「产地」 | 浅烘在前；产地按名字排 | 待测 |
| 4 | 点开一张卡 | 最近一袋那行也有每克价；没有还剩多少 | 待测 |

# 🚧 进行中：2026-09-06 王国杯测上传照片

对应 [020](020-🚧-咖啡王国.md)。机器：没杯测先挂图会拒、别人不能删、撤回和注销清文件、一杯最多 8 张，已钉、不必测。

- **这台 Mac 开发机**：打开 [http://localhost:8000](http://localhost:8000)。改完后端已重启（新表），改完前端要刷新。本机 `data/` 和小主机不是同一份库。
- **只拿【测试】王国豆练手。不要动真库存价钱和消耗。**

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 打开王国里一支豆，记下杯测，点「传照片」选一张 | 评价下面和「改我的杯测」里都能看见这张图 | 待测 |
| 2 | 再换一个号打开同一支 | 看得见那张图，没有删除按钮 | 待测 |
| 3 | 回到自己的号，点「撤回我的」 | 杯测和照片都没了 | 待测 |

# ✅ 已完成：2026-09-05 本机隔离库模拟手测

对应 [010](010-🚧-防毁账与本地耐久.md) / [018](018-🚧-个人器具与冲煮建议.md) / [020](020-🚧-咖啡王国.md) / [012](012-✅-冲煮时长与研磨建议.md)。

用独立目录 `COFFEEBAR_DATA=/tmp/cb-mac-handtest` 打 `:8010`，模拟豆、酒、器具、王国。备份到 `/tmp/cb-mac-backups`，还原到 `/tmp/cb-mac-restore`。接口和脚本 36 项全过。仓库 `data/`、小主机、`:8000` 那份库都没动。这台环境没有浏览器工具，页面按钮未点；注册二选一、注销闸门按接口验收。

# 🚧 进行中：2026-09-06 广场筛选与 MCP

对应 [005](005-🚧-社区版与多租户.md)。机器：烘焙/处理法/标签/已进王国筛、MCP `list_plaza` / `get_plaza_bean` 带买价克重、不带剩余，已钉、不必测。

- **这台 Mac 开发机**：打开 [http://localhost:8000](http://localhost:8000)，改完前端已重编 `web/dist`。本机 `data/` 和小主机不是同一份库。
- **不要动真库存的价钱和消耗。** 用已公开的卡或【测试】卡看即可。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 侧栏进「广场」 | 搜索框下面有烘焙、处理、标签芯片（库里有对应字段才出现），还有「不看未认证」「已进王国」 | 待测 |
| 2 | 点一个烘焙芯片，再点另一个 | 只剩这两档烘焙的卡；再点标签会越点越少 | 待测 |
| 3 | 点「已进王国」，再点「清除筛选」 | 先只剩进过王国的；清除后芯片和搜索都松开，「不看未认证」还在 | 待测 |
| 4 | 点开一张公开卡 | 能看见买价和袋上克重；没有还剩多少 | 待测 |

# 🚧 进行中：2026-09-06 认证看完整档案

对应 [005](005-🚧-社区版与多租户.md)。机器：审核详情带照片/杯测/描述/进价、不带剩余和流水，已钉、不必测。

- **这台 Mac 开发机**：打开 [http://localhost:8000](http://localhost:8000)，改完前端已重编 `web/dist`。本机 `data/` 和小主机不是同一份库。
- **不要动真库存的价钱和消耗。** 用已公开的卡或【测试】卡看即可。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 管理员进「后台」→「审豆卡」，点一张已公开的卡（晨曦焦糖或 MATYAZO 也可） | 右边能看见产地各栏、描述、进价、照片、杯测雷达；顶上有「有/没」芯片 | 待测 |
| 2 | 看左边列表 | 缺照片/杯测/描述的卡会用红字标缺口 | 待测 |
| 3 | 广场打开同一张卡 | 能看见买价和袋上克重；没有还剩多少 | 待测 |

# 🚧 进行中：2026-09-06 防毁账（备份 / 接手 / 注销）

对应 [010](010-🚧-防毁账与本地耐久.md)。机器：打包还原豆名对得上、孤儿注册要 take/leave、第二人不能 take、空号可直接注销、有豆号必须带导出 token、事务中途失败回滚，已钉、不必测。

- **这台 Mac 开发机**：`data/` 和小主机不是同一份。备份演练用临时目录，不要盖本机 `data/`。2026-09-05 已用 `/tmp/cb-mac-handtest` 走完 1–6（#3/#4/#5 按接口，未点页面）。
- **小主机**：真库存号、格兰杰、四支真豆不要拿来试 `leave` 或真注销。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 设 `COFFEEBAR_BACKUP_DIR` 指向第二块盘或临时目录，跑 `bash scripts/backup.sh`（小主机双击 `backup.bat`） | 那个目录出现当天 zip；没设环境变量时脚本仍成功，并提示「还在本机」 | 已测通过 |
| 2 | `bash scripts/restore.sh 那份.zip --dest /tmp/cb-restore --force`（不要指到真 `data/`） | 临时目录里能打开 `coffeebar.db`，豆数对得上 | 已测通过 |
| 3 | 只在**空的临时库**上：先放没主人的豆，再注册、不选接手 | 页面问「接手吧台账本 / 只要空库」；库还没被领走 | 已测通过 |
| 4 | 同一临时库点「接手吧台账本」 | 进得去，豆挂在这个号上 | 已测通过 |
| 5 | 小主机已登录的真库存号：侧栏点注销，不先下载备份 | 「确认注销」点不了或提交被拒「先导出备份」。**不要真注销** | 已测通过 |
| 6 | 用空号或【测试】号走一遍注销 | 空号不用下载就能注销；有豆的【测试】号要先下载备份 | 已测通过 |

# 🚧 进行中：2026-09-06 咖啡王国

对应 [020](020-🚧-咖啡王国.md)。机器：普通人不能收录、一人一豆可改可撤、两张公开卡可挂同一支、收藏开关、注销留目录、MCP 杯测，已钉、不必测。

- **这台 Mac 开发机**：`bash scripts/start.sh`，打开 [http://localhost:8000](http://localhost:8000)。改完后端要重启（新表），改完前端要 `cd web && npm run build` 再刷新（或用 `scripts/dev.sh`）。本机 `data/` 和小主机不是同一份库。
- **小主机**：`git pull` → `install.bat` → `start.bat`。

**只拿【测试】公开卡练手。不要动真库存豆的价钱和消耗。**

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 用普通号建一张【测试】豆，公开到广场 | 广场能看见；侧栏「王国」还没有这支 | 已测通过 |
| 2 | 管理员进后台「收进王国」，新建一支（或挂到已有同名豆） | 待收入少一张；王国列表出现；主人若在私卡打过杯测，王国里已有他那一条 | 已测通过 |
| 3 | 换另一个号打开这支，打八维分和一句评价，再改一次 | 只有一条是你的，标「（我）」；均分会变；看不见进价和还剩多少 | 已测通过 |
| 4 | 点收藏，回列表开「只看收藏」 | 只剩这支；再点一次收藏会取消 | 已测通过 |
| 5 | 广场打开那张公开卡 | 有「在王国里看大家的杯测」 | 已测通过 |

# 🚧 进行中：2026-09-06 更新页日期倒序

对应 [008](008-🚧-版本更新.md)。机器：`changelog.js` 条目倒序、乱序输入仍按天新→旧，已钉、不必测。不改库存。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 侧栏点「更新」，看最上面一张卡片的日期 | 是 2026-09-06（或当天最新一天），9 月 5 日在它下面 | 机器已钉、不必测 |

# 🚧 进行中：2026-09-05 冲煮时长与研磨建议

对应 [012](012-✅-冲煮时长与研磨建议.md)。机器：V60 慢 20 秒粗一点、慢 10 秒先别动、两杯偏慢出提示、撤回后消失，已钉、不必测。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 用【测试】豆打开冲煮指导，播完或手填一个比建议慢 20 秒以上的总秒，记下 | 冲煮记录那一行有实际时间，还有「方案 … · 偏慢」 | 已测通过 |
| 2 | 同样方式再记一杯明显偏慢，刷新豆卡 | 冲煮指导下面出现「下次粗一点」 | 已测通过 |
| 3 | 把这两杯撤回 | 那句话消失 | 已测通过 |

不要拿真库存豆练手。测完撤回或删【测试】卡。

# 🚧 进行中：2026-09-05 个人器具与冲煮建议

对应 [018](018-🚧-个人器具与冲煮建议.md)。机器：账号隔离、锥形/平底推荐、管理员收录拷图、注销留目录、MCP 登记滤杯，已钉、不必测。

- **这台 Mac 开发机**：`bash scripts/start.sh`，打开 [http://localhost:8000](http://localhost:8000)。改完前端要 `cd web && npm run build` 再刷新（或用 `scripts/dev.sh`）。本机 `data/` 和小主机不是同一份库。
- **小主机**：`git pull` → `install.bat` → `start.bat`。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 侧栏点「器具」，登记一只平底滤杯，形状选平底，可传一张图 | 台面上出现这张卡 | 已测通过 |
| 2 | 打开任意豆卡的冲煮指导，看方式下拉 | Kalita 带「你有」；V60 带「缺滤杯」；上面有一句更适合 Kalita | 已测通过 |
| 3 | 再登记一只锥形滤杯 | V60 / 火山冲也会标「你有」 | 已测通过 |
| 4 | 管理员号打开后台「收器具」，把刚才那件收到目录，写一句冲煮备注 | 待收录列表少一件；目录里出现；再换一个号能「领到台面」 | 已测通过 |
| 5 | 不要拿真库存豆卡练手。器具登记不改豆子账 | 豆库克重不动 | 已测通过 |

# 🚧 进行中：2026-09-05 公开豆卡 / 认证 / 广场

对应 [005](005-🚧-社区版与多租户.md)。机器：默认私密不上广场、公开去敏、普通人不能认证、管理员能认证、改产地掉认证、钉不对先拒再 force、MCP 审核工具要管理员，已钉、不必测。2026-09-05 本机再用 MCP 客户端对跑着的服务自测 27 项全过（临时号建卡/公开/去敏/审过/掉认证/收回/无钉先拒，测完已注销）。真豆卡四张未动。Cursor 里那套 MCP 仍是旧进程，且仓库 `.env` 口令对不上本机库，不能直接当审核号用。

**这台 Mac 开发机**：改完后端要重启 `start.sh`，改完前端要 `cd web && npm run build` 再刷新。本机 `data/` 和小主机不是同一份库。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 用**非管理员**号新建一张【测试】豆，默认不要勾公开 | 豆库有这张卡；侧栏「广场」看不见它 | 待测 |
| 2 | 打开这张卡，点「公开这张卡」 | 提示已公开；广场能看见名字和产地。买价和袋上克重后来补上了，见上面「广场买价和克重」 | 已测通过 |
| 3 | 广场打开这张卡 | 能看照片/产地/落点；没有袋子和谁喝的。标「未认证」 | 待测 |
| 4 | 广场点「不看未认证」 | 这张未认证卡消失 | 待测 |
| 5 | 管理员号进侧栏「后台」→「审豆卡」，或用管理员 `.env` 让 Cursor 调审核 MCP | 队列里有这张卡；能看到当前钉和词典对照，也能看到照片/杯测/描述/进价 | 待测 |
| 6 | 审过并认证（钉不对就先「按词典重钉」或手改） | 广场上这张卡出现「已认证」；开「不看未认证」也还在 | 已测通过 |
| 7 | 回到主人号，豆卡「改资料」把产地改掉 | 认证掉了；广场仍公开但变回未认证 | 待测 |
| 8 | 点「改回只自己看」 | 广场不再出现这张卡 | 待测 |

测完把【测试】卡删掉或改回只自己看。不要动真库存的价钱和消耗。

# 🚧 进行中：2026-09-06 改密码 / 补货带酒

对应 [005](005-🚧-社区版与多租户.md)、补货页。机器：改密码踢掉其他会话、酒不够一杯进补货、对照图带缩略图，已钉、不必测。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 侧栏点「改密码」，旧密码对、新密码输两遍一样 | 提示改好了；这台还登着 | 待测 |
| 2 | 用一张见底的【测试】酒（剩不到 30 ml）打开补货 | 酒水一节出现这支；点进去是酒卡 | 待测 |
| 3 | 补货页豆子那一行点「对照图」传一张 | 左边圆点变成图。不要动真库存豆卡正文 | 待测 |
| 4 | 豆库搜一个名字，酒库搜一个风味 | 列表立刻变少，对得上的还在 | 待测 |
| 5 | 手机顶栏图标若挤不下 | 能横着滑到日历、地图、更新 | 待测 |
| 6 | 酒单搜一个酒名或材料 | 对得上的还在 | 待测 |
| 7 | 画像搜一个人名 | 芯片变少 | 待测 |

# 🚧 进行中：2026-09-06 登录可以试错 5 次

对应 [005](005-🚧-社区版与多租户.md)。机器：登对了不占次数、网页满 5 次错才 429、MCP 连错不影响网页，已钉、不必测。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 退出，故意把密码输错 5 次 | 前 5 次都是「邮箱或密码不对」，还能再点登录 | 待测 |
| 2 | 第 6 次再错 | 提示「登录试错已经 5 次，过一分钟再来」 | 待测 |
| 3 | 等一分钟（或把服务关了再开），用对的密码登录 | 能进去 | 待测 |

# 🚧 进行中：2026-09-06 删除酒卡

对应 [004](004-🚧-基酒第一期.md)。机器：无流水直接删、有倒过不带 mode 409、`keep` 留钱、`wipe` 清钱、不误删别人、占锁 423、酒单/配方引用不炸、删卡后酒单条目拿掉，已钉、不必测。本机 2026-09-05 用临时空号经 MCP 客户端又跑了一遍 keep/wipe/摘酒单，格兰杰未动。

- **这台 Mac 开发机**：`bash scripts/start.sh`，打开 [http://localhost:8000](http://localhost:8000)。本机 `data/` 和小主机不是同一份库。
- **小主机**：`git pull` → `install.bat` → `start.bat`。

**只拿【测试】酒练手。不要 wipe 格兰杰，也不要拿真库存当连记录一起抹的对象。** 库里若还有【测试】金酒 / 波本 / 双瓶金，正好用来删。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 打开一张没倒过的【测试】酒卡，点右上「删除酒卡」 | 确认框在正中；只有「不删了 / 删掉酒卡」 | 待测 |
| 2 | 点「不删了」 | 卡还在 | 待测 |
| 3 | 再删 →「删掉酒卡」 | 回到酒库，这张卡没了；切「历史」也没有 | 待测 |
| 4 | 另开一张【测试】酒，倒一杯，再点删除 | 出现「不删了 / 留下花掉的钱 / 连记录一起抹」，不用先撤回 | 待测 |
| 5 | 选「留下花掉的钱」 | 酒库没这张卡；打开统计，那一杯的钱和杯还在 | 待测 |
| 6 | 再建一张【测试】酒，倒一杯 →「连记录一起抹」 | 酒库没卡；统计里这张卡的钱和杯也没了。不要动格兰杰 | 待测 |
| 7 | 若酒单上还有这支【测试】酒的纯饮/鸡尾酒 | 删卡之后出品面看不见这些条目 | 待测 |

# 🚧 进行中：2026-09-06 推荐酒单（纯饮 + 鸡尾酒）

对应 [004](004-🚧-基酒第一期.md)。机器：`uv run pytest` 204 passed。隔离、锁、杯数去重已钉住。

- **小主机**：`git pull` → `install.bat` → `start.bat`。
- **这台 Mac 开发机**：`bash scripts/start.sh`，打开 [http://localhost:8000](http://localhost:8000)。本机 `data/` 和小主机不是同一份库。

**测完用【测试】配方/条目。不要动格兰杰真库存当 wipe 对象。** 酒卡现在能删了，测完的【测试】卡可以直接删（真喝过选留下钱；练手卡可选连记录一起抹）。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 侧栏「酒水」下出现「酒单」，点进去 | 出品面能看见已上架条目；点「编辑」能看见下架的 | 已测通过 |
| 2 | 编辑 → 上架纯饮（【测试】酒，不要用格兰杰）→ 出品 → 倒一杯，毫升改成 40 | 酒卡剩余少 40 ml；统计酒杯 +1 | 已测通过（本机库，已撤回） |
| 3 | 上架鸡尾酒（两支【测试】基酒），倒的时候把两支毫升都改掉 | 两瓶都按改后的数扣；日历当天是 1 条鸡尾酒，下面列出两支毫升 | 已测通过（出品/撤回）；日历是否一巡一张见 #6 |
| 4 | 同一支【测试】酒开两瓶未关，从酒单倒纯饮且不选瓶 | 提示要选第几瓶、不自挑、库存不动 | 机器已钉、不必测 |
| 9 | 库里两支【测试】金酒，酒单纯饮默认甲，倒的时候改选乙 | 出现「用哪支金酒」；扣乙不扣甲 | 机器已钉、不必测 |
| 5 | 把刚才那巡在酒卡或流水里撤回 | 两瓶都加回去；统计杯数少 1，不是少 2 | 已测通过 |
| 6 | 日历点开倒鸡尾酒那天 | 一巡一张，不按材料行数加点子 | 机器已钉、不必测 |
| 7 | 把【测试】条目下架 / 拿掉 | 出品面看不见；拿掉后配方不再摆在酒单上。不要 wipe 格兰杰 | 机器已钉、不必测 |
| 8 | 出品倒一杯时点选人名，再多选两人 | 芯片能点；两人就是两杯、库存按两倍扣；日历两张各写一个人 | 已测通过 |
| 10 | 编辑 → 上架鸡尾酒 → 先选已有【测试】配方，把里面基酒换成另一支，保存 | 酒单上这款的材料变成新选的酒；再点「改配方」也能换基酒 | 已测通过 |

机器已钉、不必测：账号隔离、网页占锁时 MCP 整巡不写、老酒卡「倒一杯」仍按 1 杯计；MCP 换配方基酒 / 查删配方 / 拿掉酒单。

# 🚧 进行中：2026-09-05 豆子产地地图

对应 [007](007-🚧-豆子地图.md)。机器：`uv run pytest` 182 passed。

- **小主机**：`git pull` → `install.bat` → `start.bat`（前端依赖多了地球组件，要重新装并构建）。
- **这台 Mac 开发机**：`bash scripts/install.sh`（已装过可跳过）→ `bash scripts/start.sh`，打开 [http://localhost:8000](http://localhost:8000)。本机 `data/` 和小主机不是同一份库。

**别删真库存那 6 支。** 用真豆看落点即可；纠正位置只点假卡或自己愿意改的那张。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 侧栏出现「地图」，点进去 | 能看到世界地图和右侧豆列表 | 待测 |
| 2 | 切「墨卡托 / 等积 / 地球」 | 三种都能看，钉还在对应产地附近；地球能转 | 待测 |
| 3 | 看真库存 6 支 | 西达玛、蕙兰、迪马、南米纳斯、恩戈罗雷罗、晨曦焦糖（四个钉）能在图上找到 | 待测 |
| 4 | 点一颗钉 | 进对应豆卡；豆卡上能看到「地图上：…」 | 待测 |
| 5 | 建一张「【测试】无产地」假卡，去地图选中，在图上点一下 | 出现手定点；豆库里其他豆还在 | 待测 |
| 6 | 承上：点「用词典重猜」 | 这张假卡对不上词典，会从地图上拿掉、进「还没定点」 | 待测 |
| 7 | 手机窄屏打开地图 | 图在上、列表能滚，三种投影能切 | 待测 |
| 8 | 鼠标移到一颗钉上（三种投影各试一次） | 弹出卡片：产地、经纬度、产区、豆子名、克重、风格 | 待测 |
| 9 | 拉大 / 缩小浏览器窗口，再试手机窄屏 | 地图铺满剩下的高度，国界跟着容器重算，不要固定一块小画布 | 待测 |
| 10 | 鼠标移到埃塞 / 巴西 / 哥伦比亚国土上（三种投影各试一次） | 弹出产地百科：海拔、豆种、风味、名产；库里没这国的豆也能出 | 待测 |
| 11 | 鼠标移到西达玛 / 耶加雪菲 / 蕙兰那种空心圆环上 | 弹出的是产区百科，不是某张豆卡；再移到实心钉上变成豆子详情 | 待测 |
| 12 | 滚轮放大东非 / 巴西那几颗叠在一起的钉，分别点每一颗 | 圆点屏幕大小几乎不变；挤在一起的会围成一圈，每颗都能悬停、点进对应豆卡 | 待测 |
| 13 | 关掉「豆子」，只留「产区产地」 | 豆子钉消失，国家高亮和圆环还在，能悬停百科；不能两层一起关 | 已测通过 |
| 14 | 再关掉「产区产地」、打开「豆子」 | 圆环和国家百科没了，只剩豆子钉 | 已测通过 |

词典匹配、拼配四钉、手定点不被改产地冲掉、别人的豆 404、国家/产区百科字段、重叠钉散开算法，机器已钉、不必测。

# 🚧 进行中：2026-09-06 MCP

对应 [003](003-🚧-豆子第一期实现.md)「MCP」。网页服务要先开着。仓库根目录复制 `.env.example` 为 `.env`，填这个号的邮箱和密码。Cursor 打开本仓库后应出现 `coffeebar` MCP。

机器：未运行、占锁硬拒、多袋不自挑、出表写文件，已钉、不必测。

Agent 在本对话里把 50 个工具各跑通一次（只动【测试】卡和人，测完已清掉）。2026-09-05 本机再测：同一套 MCP 客户端用临时空号 12 项全过（删酒卡 keep/wipe、配方换基酒、同类换支、多瓶不自挑、摘酒单），测完已注销。Cursor 工具已绿，但 `.env` 口令对不上本机库，不能直接改你的真库存。网页占锁硬拒、服务停掉这两条仍要人手。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | Cursor 里看到 coffeebar MCP 已连接 | 工具列表里有 list_beans、record_brew、calendar_month。Mac 上若红，多半是旧配置写死了 Windows 路径，拉代码后刷新 MCP | 已测通过 |
| 2 | 对 Cursor 说「豆库还剩哪些」 | 能报出和网页豆库对得上的名字（本机库，不是小主机真库存） | 已测通过 |
| 3 | 网页打开一张豆卡编辑着，再让 Cursor 给这张卡记一次冲煮 | 被拒，说明谁在编辑；网页那张卡没被改 | 待测 |
| 4 | 说「建一张【测试】MCP 豆，200g 80 元」，再刷新豆库 | 出现这张卡。测完可删（会改本机库，别拿真库存号乱 wipe） | 已测通过 |
| 5 | 服务停掉，再让 Cursor 查豆库 | 说 coffeebar 未在运行 | 待测 |

# 🚧 进行中：2026-09-06 日历与 CSV 出表

对应 [003](003-🚧-豆子第一期实现.md)「CSV 出表与日历」。**不改库存。** 开发机 `data/` 和小主机真库存不是同一份。

机器：`uv run pytest` 188 passed。凌晨 4 点分日、撤回不进月份点数、zip 带 BOM、别人账号看不到，已钉、不必测。

**这台 Mac**：若 `start.sh` 已在跑，先停再开一次（出表接口是新的）；强制刷新才能看到新页面。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 侧栏出现「日历」，点进去 | 本月格子，周一在最左；上点咖啡、下点酒 | 已测通过（复看上下点） |
| 2 | 点一个有点子的日子 | 下面列出当天豆/酒、谁、克或毫升、钱 | 已测通过（库里还没酒的消耗，只看到咖啡） |
| 3 | 点「上个月 / 下个月」 | 格子跟着换；没有记录的日子点开是空的 | 已测通过 |
| 4 | 用上面的人名芯片滤一个人 | 只剩他的点子；再点「全部」恢复 | 已测通过 |
| 5 | 日历右上「出表 CSV」 | 下载 zip；解开能用 Excel 打开，中文表名在 | 已测通过（zip 里 10 张中文表） |
| 6 | 统计页换「本周 / 本月」再出表 | 也能下到 zip，期间对得上 | 已测通过 |
| 7 | 画像里点「看他的日历」 | 跳到日历，已经按这个人滤着 | 已测通过 |
| 8 | 手机窄屏点顶部日历图标 | 同样能打开月历 | 未测 |

# 🚧 进行中：2026-09-06 版本更新页

对应 [008](008-🚧-版本更新.md)。不改库存。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 侧栏出现「更新」，点进去 | 看到按日期倒序的白话条目 | 已测通过 |
| 2 | 手机窄屏点顶部卷轴图标 | 同样能打开更新页 | 待测 |
| 3 | 拉大 / 缩小浏览器看「更新」 | 始终一列，卡片铺满侧栏右边；同一天的几条在一张卡片里 | 待测 |

# ✅ 已完成：2026-09-05 账号隔离与登录加固（小主机）

对应 [005](005-🚧-社区版与多租户.md) 账号归属 + 登录加固。机器：`uv run pytest` 当时 160 passed。

| # | 做什么 | 状态 | 备注 |
| - | ---- | ---- | ---- |
| 1 | 豆子「在库 / 历史」原来的豆还在（当时 6 支） | 已测通过 | 用户回报 |
| 2 | 酒水原来的酒瓶还在 | 已测通过 | 用户回报 |
| 3 | 统计没被清空 | 已测通过 | 用户回报 |
| 4 | 退出再用同一邮箱密码登录，库还在 | 已测通过 | 用户回报 |
| 5 | 忘记密码：没配邮箱时出现「本机没配邮箱，点这里继续」，能重设 | 已测通过 | 用户回报，截图确认 |
| 6 | 改密前另一台设备也登录着，改完那边刷新被踢回登录 | 未测 | 用户说先等等 |
| 7 | 第二个邮箱注册后豆库/酒水/统计是空的 | 已测通过 | 用户回报 |

# 🚧 进行中：2026-09-05 注销与上传限流

对应 [005](005-🚧-社区版与多租户.md)「上传限流与注销删数据」。提交 `d00912c`。机器：`uv run pytest` 162 passed（含注销只清自己、第 21 张上传 429）。

**先**：小主机 `git pull` → `install.bat` → `start.bat`。侧栏（手机顶栏也有）应出现「注销账号」。

**警告**：下面注销条目只用**空号**（第二个注册的那个）。**不要**用接手 6 支豆的号注销。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 空号登录，点「注销账号」，邮箱填错、密码填对 | 提示请输入这个账号的邮箱，豆还在 | 已测通过 |
| 2 | 同一对话框，邮箱对、密码错 | 提示密码不对，豆还在 | 已测通过 |
| 3 | 邮箱和密码都对，确认注销 | 回到登录页；这个邮箱再登录失败；再注册是空库 | 失败 |
| 4 | 用接手真库存的那个号登录 | 6 支豆、酒、统计都还在 | 未测 |
| 5 | 在真库存号上给一张豆卡传一张包装照 | 能挂上，页面看得到 | 未测 |
| 6 | 同一账号一分钟内传满 21 张照片 | 第 21 张被挡 | 机器已钉、不必测 |
| 7 | 补测：改密后另一台设备被踢（上一轮 #6） | 那边刷新回登录页 | 未测 |

#3 失败：确认注销后页面报 `Unexpected token 'I', "Internal S"... is not valid JSON`（服务端 500 纯文本）。原因：注销接口同时开了两根 SQLite 连接，Windows 上删账号容易锁库；前端又按 JSON 去解析。已修，见下一节。

# 🚧 进行中：2026-09-05 注销 500 修复后复测

修了两根连接锁库、老库外键删除顺序，以及非 JSON 错误不再把页面弄成 Unexpected token。机器：`uv run pytest` 163 passed。小主机再 `git pull` → `install.bat` → `start.bat`。

**仍然只用空号注销。不要用接手 6 支豆的号。**

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 空号登录，注销，邮箱和密码都对，确认 | 回到登录页，不要再出现 Unexpected token / Internal Server | 失败 |
| 2 | 刚注销的邮箱再登录 | 失败（邮箱或密码不对） | 待测 |
| 3 | 同一邮箱重新注册 | 能进，豆库是空的 | 待测 |
| 4 | 用接手真库存的号登录 | 6 支豆、酒、统计都还在 | 待测 |
| 5 | 在真库存号上给一张豆卡传一张包装照 | 能挂上 | 待测 |
| 6 | 改密后另一台设备被踢 | 那边刷新回登录页 | 未测 |

#1 失败：提示 `注销没做成：no such table: main._old_person`。老库重建 `person` 后，消耗流水的外键还指着已经删掉的临时表。启动时会重修这张表；注销时暂时关掉外键。

# ✅ 已完成：2026-09-05 注销 _old_person 修复后复测

小主机 `git pull` → `install.bat` → `start.bat`（这次启动会修库）。机器：`uv run pytest` 165 passed。**只用空号注销。**

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 空号登录，注销，邮箱和密码都对，确认 | 回到登录页，不要再报 _old_person | 已测通过 |
| 2 | 刚注销的邮箱再登录 | 失败 | 已测通过 |
| 3 | 同一邮箱重新注册 | 能进，豆库是空的 | 已测通过 |
| 4 | 用接手真库存的号登录 | 6 支豆、酒、统计都还在 | 已测通过 |
| 5 | 在真库存号上给一张豆卡传一张包装照 | 能挂上 | 未测 |
| 6 | 改密后另一台设备被踢 | 那边刷新回登录页 | 未测 |

注销这条闭环：三次失败（500 纯文本 → 可读报错 → `_old_person`）后 1–4 全通。**留两条尾巴**：#5 传照片没测（上传接口这轮加了限流，改动过）；#6 一直没有第二台设备。下轮交付时把这两条带上。

# 🚧 进行中：2026-09-05 删豆卡留下钱或连记录抹

上一轮确认：有未撤回记录会挡删、撤回后能删、删后别的豆还在。这轮改成确认框里两选，不必先撤回。

**只拿假卡练手，别删真库存那 6 支。** 虚构日晒若已删掉，再建一张「【测试】…」即可。小主机 `git pull` → `install.bat` → `start.bat`。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 假卡冲一次，点「删除豆卡」 | 确认框在正中；出现「不删了 / 留下花掉的钱 / 连记录一起抹」，不用先去撤回 | 待测 |
| 2 | 点「不删了」 | 卡还在，那笔冲煮还在 | 待测 |
| 3 | 再点删除 →「留下花掉的钱」 | 豆库和「历史」都没这张卡；打开统计，喝掉的钱和杯还在 | 待测 |
| 4 | 再建一张假卡，冲一次 →「连记录一起抹」 | 豆库没卡；统计里这张卡的钱和杯也没了 | 待测 |
| 5 | 真库存号：确认 6 支豆一支没少 | 都在 | 待测 |
| 6 | 真库存号：给任意一张真豆卡传一张包装照 | 能挂上 | 待测 |
| 7 | 改密后另一台设备被踢 | 那边刷新回登录页 | 未测 |
| 8 | 手机窄屏再点一次「新建豆子」 | 框仍在视口里，能滚完表单 | 待测 |

不带 mode 拒删、`keep` 后 `spent` 还在、`wipe` 后 `spent` 归零，机器已钉、不必测。

# ✅ 已完成：2026-09-05 删除豆卡（先撤回再删）

对应 [003](003-🚧-豆子第一期实现.md) 当时「有未撤回就 409」。用户手测：打开虚构卡能看见删除；有记录会提示先撤回；撤回后能删；删后别的豆还在。交互已改成上一节的两选，本清单不再测。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 打开「【测试】虚构日晒」 | 右上出现「删除豆卡」；雷达和假评语看得到 | 已测通过 |
| 2 | 给这张卡传一张包装照 | 能挂上、能点开放大 | 待测 |
| 3 | 点「删除豆卡」→ 确认 | 回到豆库，这张卡没了；切「历史」也没有 | 已测通过 |
| 4 | 再建一张卡，冲一次（记一笔消耗），然后点删除 | 删不掉，提示先撤回那几笔 | 已测通过 |
| 5 | 承上：把那笔撤回，再点删除 | 删得掉 | 已测通过 |
| 6 | 真库存号：给任意一张真豆卡传一张包装照 | 能挂上（上一轮遗留 #5） | 待测 |
| 7 | 真库存号：确认 6 支豆一支没少 | 都在 | 已测通过 |
| 8 | 改密后另一台设备被踢 | 那边刷新回登录页 | 未测 |

# 🚧 进行中：2026-09-05 新建豆子弹出框贴左上角

Chrome 里「新建豆子」贴到视口左上角。Tailwind 把 `dialog` 的 `margin` 清成 0，`showModal()` 居中失效。所有用 Modal 的框（注销、删豆卡、冲一次、再入一袋）都会这样。已在 `web/src/index.css` 补回居中。小主机 `git pull` → `install.bat` → `start.bat`。

| # | 做什么 | 期望 | 状态 |
| - | ---- | ---- | ---- |
| 1 | 豆库点「新建豆子」 | 框在屏幕正中，背后变暗，不要贴左上角 | 已测通过 |
| 2 | 打开豆卡，点「删除豆卡」 | 确认框也在正中；点「不删了」卡还在 | 已测通过 |
| 3 | 点「冲一次」「再入一袋」 | 框也在正中 | 已测通过 |
| 4 | 手机窄屏再点一次「新建豆子」 | 框仍在视口里，能滚完表单、能点「加入豆库」 | 待测 |
