# 023 · 挂耳咖啡

> **状态**：🚧 —— 侧栏栏目、按包扣、桔林烛光这一包已能跑。盒装一共几包、多少钱还没入。
> **范围**：挂耳不走进手冲豆的克重剩余；消耗一包就是一包；豆库列表不混挂耳。
> **前置**：豆卡 / 批次 / 消耗见 [002](002-🚧-豆子档案与小主机架构.md)、[003](003-🚧-豆子第一期实现.md)。

## 已定（不要重开）

- `bean.form`：`beans`（默认，手冲豆）或 `dripbag`（挂耳）。
- 挂耳库存是 **包数**：`packs - COUNT(未撤回消耗)`，不走克重 `BALANCE`。
- 冲一包：`amount_g` 强制写成袋上净含量（默认 8 g），只作标注和算钱；`unit_cost = price / (packs × 净含量)`，`pack_cost = price / packs`。没填价钱不用猜。
- 不扣滤纸、不盘点克重、不开袋实称、没有整袋补录。
- `GET /api/beans?form=beans|dripbag`，豆库默认仍只出手冲豆。
- 全局平均粉量排除挂耳，避免 8 g 拉低 15 g。
- 补货：挂耳剩 0 包才上「挂耳没有了」。
- 价钱、盒装包数读不清就空着，不要猜。

# ✅ 已完成：侧栏栏目与按包扣

## 目标

挂耳和手冲豆不是同一种库存。侧栏单独「挂耳咖啡」，冲一包扣一包，列表和豆卡都不谈「还能冲几杯克重」。

## 落地

- 表：`bean.form`、`bean_lot.packs`；老库启动补列。
- 建卡 `form=dripbag` 带 `packs` 就入第一批；不带包数是待入，跟着在库出。
- 冲煮忽略客户端粉量，强制一包净含量；撤回加回一包。
- 网页：侧栏「挂耳咖啡」；列表按包；豆卡「冲一包」，不出现冲煮指导、开封动画、克重盘点。
- MCP：`list_beans` / `create_bean` / `create_bean_lot` 认得 `form`、`packs`。

## 验收

`uv run pytest tests/test_dripbag.py`。手测见 [006](006-🚧-手工验收.md)。

# ✅ 已完成：录入桔林烛光·低因

## 读到的字段（包装图）

- 名字：桔林烛光·低因
- 英文：MANDARIN FOREST CANDLELIGHT DECAFFEINATED
- 品牌：MILANGGOLD
- 形态：挂耳 / 焙炒咖啡
- 每包净含量：8 g
- 包装刻度：香气 4.0、浓度 3.5、酸度 1.0、烘焙度 3.5 → 烘焙记中烘
- 产地、豆种、海拔、处理法、烘焙日、购入日、价钱、盒装一共几包：包装上看不到，**空着**

## 写入

真库存号（`1821601734@qq.com`，`owner_id=1`）豆卡 **id=27**，`form=dripbag`，第一批 **1 包**（照片这一袋），无价。品牌记在处理厂 `MILANGGOLD`，备注是英文名和包装刻度。封面挂包装图 `kind=pack`。再买一盒用「再入一批」补包数和价钱。不要对这张卡重复建档。

# ⌛️ 未完成：盒装批次（多包 + 价钱）

一盒几包、多少钱，人说了再补。不要按电商页去猜。
