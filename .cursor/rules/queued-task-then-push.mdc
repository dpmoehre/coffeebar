---
description: 每完成一条队列任务后提交并推送 Git（本仓库）
alwaysApply: true
---

# 队列任务完成后推送版本

在本仓库中协作时：

1. **每完成用户队列（queued）中的一条任务**（例如补充文档、更新配图、改 README、修 bug 等），在收尾时：
   - 若有未跟踪或未提交的更改：执行 **`git add`** 与 **`git commit`**（使用简明中文说明，如 `docs(coffeebar): …` 或与团队惯例一致的前缀）。
   - **随后执行 `git push`**，将当前分支同步到 **`origin`**，避免本地堆积未推送提交。

2. 若用户明确表示**暂不推送**或**仅本地保存**，则尊重其选择。

3. 推送前若存在**不应入库**的本地工程改动（如仅本机 `Temp`、个人密钥），不要 `git add` 这些路径；仅推送已纳入版本控制的文档与约定工程文件。
