# TODO / 变更索引

> 按日期**倒序**（最新在上）记录精炼变更与验收索引，详细任务看板见 `docs/NNN-状态-主题.md`。
> 本文件不复制任务文档全文，只做日期分段引用；规则见 [CLAUDE.md](CLAUDE.md)。

---

## 2026-09-04

- **豆子第一期落地跑通**：从只有文档到能真跑。后端 FastAPI + SQLite（`app/`，无 ORM，`schema.sql` 幂等建表），前端 Vite + React 19 + Tailwind v4 + anime.js（`web/`，豆库 / 豆卡 / 统计 / 画像四页），部署脚本 `install|start|backup.bat` + 可选 Caddy。三条硬口径都有用例钉住：账面 = 可用克重 + Σ 库存事件 − Σ 未撤回消耗；`unit_cost` 写入即冻结（补开袋实称不回溯改写历史金额）；撤回只标 `voided_at`，已关袋批次的差额记成当天新调整。平均每杯粉量分层统计驱动全站杯数与见底判断。写锁网页可接管、MCP 硬拒。**验收**：`uv run pytest` 75 passed；`uv run python scripts/smoke.py` 对着真服务走十步全绿（含一处只有真跑 uvicorn 才暴露的 SQLite 跨线程问题）。进度见 [docs/003-🚧](docs/003-🚧-豆子第一期实现.md)，[docs/002-🚧](docs/002-🚧-豆子档案与小主机架构.md) 的落地清单已逐条标状态。**缺口**：照片、CSV 出表、日历、小主机实机验收、酒与 MCP。
- **平均每杯粉量代替固定 15 g**：消耗记当次实际用量，系统统计「我平均一杯用多少粉」（这支豆 20 杯 → 全局 50 杯 → 兜底 15 g，带区间，排除撤回）；杯数估算、见底阈值、这杯钱全站统一按它换算。刚拆袋默认按标称扣，开袋实称降为可选口子。统计页加该数字并明确所有汇总排除撤回流水。写入 [docs/002-🚧](docs/002-🚧-豆子档案与小主机架构.md)，样张豆库/豆卡/冲一次/统计已按平均粉量重算。
- **第一刀定为只做豆子 + 三条落地决定**：冲一次显式选批次（不自动 FIFO，不够粉时提示）；「谁喝的」是可增/改名/停用的实体，每人一页追踪，记错了可撤回（标 `voided_at` 不物理删，已关袋批次的差额记成当天新调整）；冲煮方案按当场输入的豆重粉重即时算，`BrewGuide` 只存默认值。事件表合并为 `StockEvent` + `ConsumptionEvent`，一天以凌晨 4 点分界。写入 [docs/002-🚧](docs/002-🚧-豆子档案与小主机架构.md)，样张豆卡加了选袋子、消耗流水撤回、人管理。
- **写锁改软锁 + 成本冻结**：网页之间提示接管不硬等超时（锁存表、5 分钟过期、60 秒心跳），MCP 写入仍硬拒绝；每笔消耗/饮酒流水落 `unit_cost` 快照，统计读快照，开袋实称与关袋偏差各记新流水，历史金额不回溯改写。写入 [docs/002-🚧](docs/002-🚧-豆子档案与小主机架构.md)，同步 CLAUDE.md 与 README。
- **统计要出数字**：统计页顶部先出豆克、酒毫升、酒精克、喝掉的钱、买进来的钱、杯数，再曲线和按人拆开。写入 [docs/002-🚧](docs/002-🚧-豆子档案与小主机架构.md)，样张统计页已改。
- **补货可上传图片**：每条补货可挂对照图（包装/货架/截图），豆子和基酒一样。写入 [docs/002-🚧](docs/002-🚧-豆子档案与小主机架构.md)。
- **基酒补风味类型与酒精度**：列表边上同时显示酒名、买入价、风味、% vol；画像酒精克数按酒精度估算。写入 [docs/002-🚧](docs/002-🚧-豆子档案与小主机架构.md)。
- **README 补全 + MCP**：说明功能、21 寸吧台、冲煮计算；约定 Cursor 经本机 MCP 建豆卡/挂照片/记消耗，与网页共用写锁。README 与 [docs/002-🚧](docs/002-🚧-豆子档案与小主机架构.md)。
- **冲煮指导按方式计算各段**：粉×比得总水；V60 / Hoffmann / 4:6 / Kalita 算出本段用水、秤到、手法、目标与功能。写入 [docs/002-🚧](docs/002-🚧-豆子档案与小主机架构.md)，样张豆卡可换方式重算。
- **样张对齐画像.png**：21 寸尺寸；侧栏用静图那套图标；豆库可新建，豆卡有雷达/冲一次/播放冲煮。 [_Doc/2026-09-04-ui-样张/index.html](_Doc/2026-09-04-ui-样张/index.html)。
- **用户画像与页面样张**：按「谁喝的」自动总结口味/常喝；可点样张与静图在 [_Doc/2026-09-04-ui-样张/](_Doc/2026-09-04-ui-样张/)。约定写入 [docs/002-🚧](docs/002-🚧-豆子档案与小主机架构.md)。
- **自由标签**：豆/酒/配方输入即打标；豆库点芯片筛选（多选为同时带这些标签）；无独立标签后台。写入 [docs/002-🚧](docs/002-🚧-豆子档案与小主机架构.md)。
- **豆库独立页**：一眼看全部剩余与进度条；可按浅/中/深烘、酸甜香、剩余、评分、地区、水温、开封日排序或筛选。写入 [docs/002-🚧](docs/002-🚧-豆子档案与小主机架构.md)。
- **包装克重与实重、袋子是否用完**：批次分记标称 / 开袋实称 / 账面；盘点校正；见底只提醒；「这袋用完」需确认并结清偏差。写入 [docs/002-🚧](docs/002-🚧-豆子档案与小主机架构.md)。
- **补给计价、酒水、日历出表、简单部署**：补货与消耗曲线、谁喝的、维度打分、价格与每杯成本；酒单/酒瓶/配方；日历与 CSV；install/start/backup。写入 [docs/002-🚧](docs/002-🚧-豆子档案与小主机架构.md)。
- **全端功能、统一动效 UI、冲煮 SVG 动画**：各端功能完整只换布局；anime.js SVG 做冲煮分步动画；写锁改为按豆卡而不是按设备砍功能。写入 [docs/002-🚧](docs/002-🚧-豆子档案与小主机架构.md)。
- **补记无多端竞争**：禁止同时改同一条数据（后改为写锁，不再按设备只读）。写入 [docs/002-🚧](docs/002-🚧-豆子档案与小主机架构.md)。
- **豆子档案与小主机架构落档**：定位为公司吧台自用豆子档案（非点单）；Win11 小主机（16GB / 7850H）单机跑 FastAPI + Vite/React + SQLite；桌面主用、手机只拍照；内网为主、cpolar 可选。正文在 [docs/002-🚧](docs/002-🚧-豆子档案与小主机架构.md)。脚手架与功能未开工。
- **仓库协作框架初始化**：从 [github.com/dpmoehre/coffeebar](https://github.com/dpmoehre/coffeebar) 克隆到本机 `001_gitea/dpmoehre/coffeebar`，落地工作牌（`.cursor/rules`）、`AGENTS.md`、`CLAUDE.md`、`todo.md`、`docs/` 看板与 `_Doc/`。产品定位已移交 002。看板 [docs/001-✅](docs/001-✅-仓库协作框架初始化.md)。
